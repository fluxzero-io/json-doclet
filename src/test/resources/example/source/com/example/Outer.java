package com.example;

/**
 * Provides nested type coverage for the doclet smoke test.
 */
public class Outer {

    /**
     * Nested type that extends the outer class to exercise inheritance handling.
     */
    public static class Inner extends Outer {
    }
}
